{
  "scripts": {
    "build": "del ./dist && mkdir dist && babel src -d dist && cp dist/icon.js ../../node_modules/antd/lib/icon/index.js",
    "test": "del ./dist && mkdir dist && babel src -d dist"
  },
  "dependencies": {
    "react": "^15.2.0",
    "react-dom": "^15.1.0"
  },
  "devDependencies": {
    "babel-cli": "^6.10.1",
    "babel-core": "^6.5.1",
    "babel-loader": "^6.2.2",
    "babel-polyfill": "^6.13.0",
    "babel-preset-es2015": "^6.5.0",
    "babel-preset-react": "^6.5.0",
    "babel-preset-stage-3": "^6.11.0",
    "del-cli": "^0.2.0"
  }
}
